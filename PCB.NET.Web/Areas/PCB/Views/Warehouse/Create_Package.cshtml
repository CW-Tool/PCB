@model PCB.NET.Web.Areas.PCB.Models.WarehouseViewModel.PackageViewModel

@{
    ViewBag.Title = "Добавить новый";
    Layout = "~/Areas/PCB/Views/Shared/_Layout.cshtml";
}

<h2>Добавить новый</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Размеры элементов</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            @Html.LabelFor(model => model.Packs, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.Packs, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.Packs, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    <a href="@Url.Action("PackageList")" class="btn btn-primary">Назад к списку</a>
</div>
